apiVersion: v1
data:
  default.conf: |
    server {
      listen 8080;
      location / {
        return 200;
      }
    }
kind: ConfigMap
metadata:
  labels:
    name: nginx-conf
  name: nginx-conf
  namespace: airlock-microgateway-monitoring
