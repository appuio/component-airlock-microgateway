parameters:
  airlock_microgateway:
    =_metadata:
      multi_tenant: true
      multi_instance: true

    default:
      namespace:
        labels:
          appuio.ch/waf: airlock
          vshn.microgateway/redis-sync-secret: "true"
    instances: {}
